server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/filestore
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
